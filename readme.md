# Content
## Code

Rn this repositroy contains a app.py file with a simple hello world inside


## Dockerfile
The dockerfile is lowkey the most simple thing ever, with a python image and a WORKDIR and copies the code. Then it runs the code via CMD

## Docker Compose
Just a simple docker compose file that uses the Dockerfile and runs a command to start the script
